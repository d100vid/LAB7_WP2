name: Lab 7

# The pipeline should be triggered only on push to a "test" branch
on:
  push:
    branches:
      - test

jobs:
  test:
    runs-on: ubuntu-latest

    # GitHub Secret passed as environment variable for Lab7Test.java
    env:
      MY_SECRET_KEY: ${{ secrets.MY_SECRET_KEY }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - name: Grant execute permission for Gradle Wrapper
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      # Run backend tests in the pipeline
      - name: Run backend tests
        run: ./gradlew test

      # Run the Jacoco report in the pipeline
      - name: Generate JaCoCo coverage report
        run: ./gradlew jacocoTestReport

      # Optional: upload the JaCoCo HTML report so it can be downloaded from GitHub Actions
      - name: Upload JaCoCo report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: build/reports/jacoco/test/html
